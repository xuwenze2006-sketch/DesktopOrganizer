# DesktopOrganizer v1.12.15 回收站专属小组件报告

## 目标

将回收站从普通桌面图标体系中剥离，建立独立的系统功能卡片，使用户内容与系统操作入口在视觉和交互上分层，减少桌面杂乱感。

## 界面与交互

- 新增 252 × 122 DIP 的圆角半透明卡片，包含系统回收站图标、状态点、项目数量、占用空间、“打开”和“清空”按钮。
- 双击卡片空白区域或点击“打开”进入系统回收站。
- “清空”先显示应用自己的二次确认，再进入后台 STA 文件任务队列；按钮和右键菜单在空、不可用或清理期间自动禁用。
- 标题区域可以拖动，按钮区域不会误触发拖动；拖动结束后保存位置。
- 可从“整理”面板中的“回收站组件”开关隐藏或恢复，右键菜单也可隐藏。
- 暂停整理时组件临时隐藏，继续整理后按用户原设置恢复。

## 状态与图标

- 每四秒在后台调用 `SHQueryRecycleBin`，读取所有磁盘回收站的项目总数和占用字节数；不枚举或读取被删除文件内容。
- 空/非空变化时只失效回收站自身的 Shell 图标缓存，使系统空桶/满桶图标同步变化；普通轮询不会重新提取图标。
- 状态不可用时保留“打开”能力，同时禁用清空操作并显示灰色状态点。
- 用户删除普通桌面文件后会额外请求一次状态刷新，不必完全等待下一次定时轮询。

## 与普通桌面内容隔离

回收站不再进入以下链路：

- `_desktopItems` 普通桌面快照；
- 自由图标与网格吸附；
- 手工/自动分类框；
- 排序、挤压排列和批量选择；
- 普通项目身份表；
- 每轮桌面 Shell 命名空间枚举。

真实桌面文件、文件夹和快捷方式继续按原逻辑扫描和整理。

## 布局与多显示器

- 布局格式升级为 Version 15，新增 `RecycleBinWidgetLayoutInfo` 保存 X/Y 坐标与显示开关。
- Version 14 及更早布局升级时，根据固定回收站 CLSID 清理旧自由图标、分组成员、自动分类恢复坐标和身份记录。
- 旧回收站若原本是自由图标，会把原坐标迁移给新卡片，再按卡片尺寸限制到可用工作区。
- 显示器重排、缩放、拔插或主屏变化时，组件随现有显示拓扑迁移算法重映射。
- 网格、智能布局和自动分组定位只在组件实际可见时把其区域视为障碍物。

## 稳定性处理

- 组件在首次桌面扫描前完成位置和可见性初始化，避免首轮自动布局把内容放到卡片下方。
- 手动清空全局图标缓存时同步重建组件图标请求，旧缓存代次不会令卡片永久停留在占位图。
- 状态轮询使用并发门，上一轮尚未结束时不叠加任务。
- 程序关闭时停止定时器并使用全局取消令牌；异步结果不会写入已关闭的 Dispatcher。
- 清空失败或取消时恢复原状态与按钮可用性，不伪造“已清空”。

## 自动化与静态验证

已完成：

- 55 个 C# 文件的字符串、注释与括号词法平衡检查；
- 7 个 XAML/XML/MSBuild/Solution 文件解析；
- GitHub Actions 与 Dependabot YAML 解析；
- 51 个 XAML 事件绑定对应处理器检查；
- 18 个资源定义和 15 个 StaticResource 引用检查；
- 327 个 MainWindow 方法签名重复检查；
- Version 15 默认值、JSON 往返和旧布局缺失字段测试；
- 回收站从普通扫描链路移除、旧坐标迁移、显示障碍物和图标缓存失效源码断言；
- Git 补丁空白检查和 ZIP CRC 完整性检查。

## 尚需 Windows 实机验证

当前容器没有 .NET 10 SDK，也不是 Windows Explorer/WPF 桌面会话，因此不能在此环境声明已经真实完成 `dotnet build` 或 Shell 交互测试。请在 Windows 上删除旧 `bin`、`obj`、`release` 后运行：

```text
run-tests.cmd
publish-win-x64.cmd
```

重点实测卡片拖动、右键菜单、空/非空系统图标、清空确认、负坐标副屏和升级后的旧位置迁移。
