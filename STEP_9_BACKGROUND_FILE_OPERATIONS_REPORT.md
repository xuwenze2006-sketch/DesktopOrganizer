# DesktopOrganizer v1.12.10 第九阶段修复报告

## 目标

把真实文件移动、删除到回收站和移动撤销从 WPF UI 线程迁移到后台串行线程，避免云盘、杀毒扫描、文件占用或 Shell 对话框导致桌面整理界面冻结。

## 根因

此前三类真实文件操作直接从鼠标或菜单事件处理器同步调用：

- `File.Move` / `Directory.Move`；
- `Microsoft.VisualBasic.FileIO.FileSystem.DeleteFile/DeleteDirectory`；
- 撤销时的反向移动。

这些入口都位于 WPF Dispatcher 线程。文件系统或 Shell 调用未返回前，输入、布局、滚动、重绘和窗口消息都无法继续处理。

## 实现

### 1. 专用 STA 串行队列

新增 `FileOperationService.cs`：

- 独立后台 STA 线程；
- FIFO 队列；
- `TaskCompletionSource` 异步回传结果；
- 请求取消与程序生命周期取消；
- 关闭时取消尚未开始任务；
- 活动原生调用不使用不安全的线程中止。

串行执行可以避免多个真实文件任务同时修改相同目录、互相覆盖同名处理或打乱撤销记录。

### 2. UI 与磁盘提交分离

新增 `MainWindow.FileOperations.cs`。UI 线程只执行：

- 路径和项目类型验证；
- 用户确认与同名策略选择；
- 捕获原分类、顺序和自由坐标；
- 标记项目为等待状态；
- 后台完成后的布局提交、刷新与提示。

真实磁盘调用只在工作线程中执行。移动任务采用“磁盘成功后提交布局”，失败或取消时不会提前删除原图标。

### 3. 路径预留与重复操作保护

使用规范化、不区分大小写的路径集合记录活动任务。等待中的真实项目：

- 半透明显示；
- 暂停鼠标命中和拖动；
- 不再作为真实文件夹投放目标；
- 禁止删除、批量删除、移出分类和重复排队；
- 增量刷新只重建受影响视觉。

### 4. 批量回收站逐项结果

批量删除不再因一个项目异常终止整批。每项分别记录成功、失败或取消：

- 只从布局清除实际成功项；
- 失败和取消项继续保留；
- 最多展示前 6 个失败详情并汇总剩余数量。

### 5. 后台撤销

Ctrl+Z 和面板按钮只负责排队。后台成功后恢复：

- 原分类；
- 原分类顺序；
- 原自由坐标；
- 自动分类前恢复坐标。

原位置被占用时不覆盖；移动后的目标不存在时移除失效记录。

## 数据与兼容性

- 程序版本：1.12.10；
- 布局格式：仍为 Version 14；
- 不新增 JSON 字段；
- 不新增第三方 NuGet；
- 最近 20 次移动撤销仍只在当前会话保存。

## 关闭边界

程序退出会取消队列中尚未开始的任务，并短暂等待工作线程收尾。已经进入 `File.Move`、`Directory.Move` 或 Windows 回收站调用的单次操作无法安全强制终止；线程为后台线程，因此不会让进程无限等待。

## 验证范围

已进行：

- 41 个 C# 文件的词法结构、括号、字符串和注释检查；
- 5 个 XAML、项目文件和 manifest XML 解析；
- 37 个 XAML 事件处理器存在性检查；
- 17 个资源键与 14 个资源引用检查；
- 308 个 `MainWindow` 方法签名重复检查；
- 真实文件移动调用位置扫描，用户文件操作仅保留在 `MainWindow.FileOperations.cs`；
- 同一路径预留、FIFO、取消与部分失败算法模拟，全部通过；
- 布局 Version 14 兼容检查；
- ZIP CRC 与 SHA-256 完整性检查。

当前环境没有可用的 .NET 10 SDK，也不是 Windows WPF/Explorer 桌面会话，因此未执行真实 `dotnet build`、回收站对话框、云盘或被占用文件实机测试。完整测试步骤已加入 `TEST_CHECKLIST.md`。
