# Changelog

## Unreleased — 本地、可恢复的桌面工作台

- 新增命名工作区及切换前预览，保存分组、自由图标、收起状态、控制面板、回收站组件、只读文件夹入口和显示器拓扑；创建、切换与恢复均不执行文件系统操作。
- 新增待整理收件箱：新项目显示建议分类、原因和可靠性，可接受建议、留在桌面、加入手工分组或稍后处理；首次完整扫描只建立基线，手工分组和低可靠度结果保持不动。
- 新增当前桌面快速搜索与未分类、近期新增、待确认、最近移动智能视图；只查询已加载项目的名称、类型、分组、标签和本地时间元数据，不扫描其它目录或文件正文。
- 新增本地标签与可解释用户规则。规则保存后保持禁用，必须依次经过预览、执行一次和显式启用；预览逐项显示命中原因、目标和冲突，取消或失败不留下布局变化，失败不自动重试。
- 新增透明 JSON 导入导出；导入规则统一回到草稿状态，标签按稳定身份过滤，不改文件名、不创建 sidecar。
- 新增永久标注“真实文件夹 · 只读”的 Folder Portal：核验根身份，只枚举当前目录直属项目，支持面包屑、打开、资源管理器定位和复制路径；禁止拖入、拖出、删除、移动和重命名真实内容，读取失败不自动重试。
- 新增独立 `%AppData%\DesktopOrganizer\operation-journal.json` 操作账本和只读操作中心。每个真实操作在进入后台队列前先持久化 `Queued`，执行前再持久化 `Running`，批量回收站操作逐项记录终态。
- 账本损坏或写入失败时 fail-closed：保留最后已落盘状态并阻止新的真实操作；启动只读核验遗留源/目标身份，收束为成功、中断或状态不确定，绝不自动重试。
- 最近 20 次成功真实移动从账本重建跨会话撤销候选；移动前保存全部命名工作区中的分组、顺序、坐标和项目元数据，撤销成功后完整回填。
- 布局格式由 Version 15 逐步升级到 Version 18，并继续兼容旧 `layout.json`：Version 16 为工作区，Version 17 为收件箱/标签/规则，Version 18 为只读 Portal。操作账本使用独立 Version 1 模型，不写入布局文件。
- 新增工作区、收件箱、搜索、规则、标签、Portal 和操作账本的纯逻辑自动化覆盖；WPF/Explorer、真实文件操作、Portal 拖放屏障以及显示器热插拔仍需 Windows 实机回归。

## 1.12.15

- 将回收站从普通自由图标、自动分类和分组系统中移出，改为独立圆角状态小组件。
- 新增项目数量与占用空间状态、空/非空系统图标切换、打开、清空、拖动定位、显示开关和位置持久化。
- 回收站状态通过后台 `SHQueryRecycleBin` 查询，清空操作进入既有 STA 文件任务队列。
- 智能布局与自由图标网格把可见回收站组件视为保留区域，降低视觉重叠。
- 布局格式升级为 Version 15；旧版回收站位置/分组身份自动清理，其他布局保持兼容。

# DesktopOrganizer 变更记录

## v1.12.14 全桌面图标防闪烁修复

- 新增 `DesktopWindowMessagePolicy`，不再把壁纸、主题、颜色等所有 `WM_SETTINGCHANGE` 广播误判为显示器变化；仅显示拓扑、DPI 与工作区变化触发布局更新。
- 显示配置消息在 180 ms 内合并，窗口边界和显示器拓扑没有实际变化时不重建视觉；显示变化不再重新扫描文件系统或清空 Shell 图标缓存。
- 普通顶级窗口的 `EVENT_OBJECT_SHOW` 不再触发三轮 Z 序校正；运行时健康检查允许临时 Shell 窗口位于 Progman 与整理层之间，避免反复移动全屏透明 layered window。
- 外部应用层级异常时只提升外部窗口，不再为修复单个窗口重新插入整个整理层。
- 用户/公共桌面目录改为完整枚举后一次性提交；枚举中途失败时保留上一份快照并退避重试，避免图标整批消失后再次出现。
- Shell 命名空间枚举新增完整性结果；暂时失败时保留上一轮回收站，不把空结果解释为删除。
- 原生桌面层巡检连续三次失败后才执行安全回退，避免单次 Explorer 瞬态导致原生图标和整理图标交替显示。
- 异步图标空结果不再覆盖已经显示的有效图标，避免真实图标与占位图之间来回切换。
- 新增显示消息过滤自动化测试；布局格式保持 Version 14。

## v1.12.13 Shell 桌面项目仅保留回收站

- 新增 `ShellDesktopItemPolicy`，使用回收站固定 CLSID `{645FF040-5081-101B-9F08-00AA002F954E}` 判断项目，不依赖中文或英文显示名称。
- Shell 桌面根枚举结果改为白名单过滤，只将回收站加入桌面快照；此电脑、网络、控制面板、用户文件、Linux 和第三方命名空间扩展不再显示。
- 用户桌面与公共桌面的真实文件、文件夹和快捷方式扫描逻辑保持不变。
- 旧布局中的已过滤 Shell 项目会在首次刷新时通过现有缺失项清理、身份表替换和增量视觉移除流程自动删除。
- 新增自动化测试，覆盖回收站 CLSID 的不同 parsing name 形式、系统语言无关判断及常见其他 Shell CLSID 的拒绝行为。
- 布局格式保持 Version 14，不新增 JSON 字段或第三方运行时依赖。

## v1.12.12 Windows CI 与自动化测试

- 新增 `.NET 10` 的 `global.json`、`DesktopOrganizer.slnx` 和统一 `scripts/ci.ps1`，本地与 GitHub Actions 共用同一条还原、编译、测试、覆盖率和发布链路。
- 新增 `windows-2025` GitHub Actions 工作流；push、pull request 和手动触发均执行 Release 构建，并将所有编译警告视为错误。
- 新增 MSTest 测试项目，覆盖本地分类器、多显示器几何、Shell 身份编码、布局 Version 14 JSON 契约及后台 STA 文件任务队列。
- 使用 Coverlet 生成 Cobertura 覆盖率；CI 上传 TRX、覆盖率文件、win-x64 单文件 EXE、ZIP 和 SHA-256 校验。
- 新增 Dependabot，对 NuGet 测试工具与 GitHub Actions 依赖执行每周更新检查。
- 新增 `run-tests.cmd` 与 `ci-local.cmd`，Windows 用户无需手写命令即可复现 CI。
- 主程序显式排除 `tests\**` 的 SDK 默认编译项，测试程序集禁用并行执行，并按 MSTest 4 分析器要求使用 `[TestMethod] + [DataRow]`。
- 本地批处理脚本保留 PowerShell 的真实失败退出码，便于终端、IDE 与上层脚本识别失败。
- 运行时项目仍无第三方 NuGet 依赖；布局格式保持 Version 14。

## v1.12.11 编译与可空性修复

- 修复 `MainWindow.DesktopIdentity.cs` 中 Shell 匹配项和文件匹配项都声明为 `match` 导致的 CS0136 编译错误。
- Watcher 重命名候选在使用前显式验证非空，消除 CS8602，同时保留失败时不迁移布局的安全语义。
- Shell 图标优先队列按 `PriorityQueue.TryDequeue` 的可空契约接收候选，并在使用前判空，消除 CS8600。
- 自由图标增量刷新按 `Dictionary.TryGetValue` 的可空契约处理目标对象，消除 CS8600。
- 不改变布局 Version 14、文件操作队列、Shell 图标逻辑或用户数据。

## v1.12.10 真实文件操作后台串行化

- 新增独立 `FileOperationService`，在后台 STA 线程中按 FIFO 顺序执行真实文件移动、回收站删除与撤销，避免云盘、杀毒扫描、被占用文件或 Shell 对话框阻塞 WPF UI 线程。
- 真实文件操作开始前按规范化路径预留项目；同一项目重复移动、删除、分类变更或撤销会被拒绝，避免交叉任务破坏布局和撤销记录。
- 排队中的图标以半透明等待状态显示并暂停命中、拖动、删除和真实文件夹投放；增量视觉指纹包含任务状态，只重建受影响图标或分类框。
- 文件移动只在后台成功后删除桌面布局项并写入撤销历史；失败或取消时恢复原视觉和布局，不再先修改界面再尝试磁盘操作。
- 批量回收站操作逐项执行并汇总成功、失败和取消结果，单项异常不会中断其余项目；只为实际删除成功的项目清理布局。
- 撤销操作也进入同一队列，执行期间禁用 Ctrl+Z 和面板按钮；原位置被占用或目标已不存在时保留明确状态并避免错误覆盖。
- 程序关闭时取消尚未开始的任务并等待后台线程短暂收尾；已经进入原生文件调用的活动任务不会被不安全地强制终止。
- 布局格式仍为 Version 14，不新增 JSON 字段或第三方 NuGet 依赖。

## v1.12.9 分类框可视区域虚拟化

- 使用固定尺寸、固定列数的 `VirtualizingGroupPanel` 替换分类框内一次性创建全部子项的 `WrapPanel`。
- 面板实现 `IScrollInfo`，只创建当前可视行、上一缓冲行和后两缓冲行中的图标；滚出范围的视觉会注销选择映射和真实文件夹投放目标；异步图标队列仅保留弱引用。
- 分类框保存完整排序快照，增量刷新复用判断不再要求所有图标同时存在于视觉树。
- 拖动分类内图标时由虚拟化面板安全取出原控件并抑制对应索引，防止布局重算时生成重复图标；拖动完成或取消后由既有刷新链路重建稳定状态。
- 记录每个分类框的运行时垂直滚动位置；分类框因增量更新或缩放重建时尽量保持原视口。
- 收起分类不创建任何子图标，也不会排队 Shell 图标请求；展开后按当前视口按需加载。
- 布局格式仍为 Version 14，不新增 JSON 字段或第三方 NuGet 依赖。

## v1.12.8 Shell 图标后台加载

- 移除 WPF UI 线程中的同步 `SHGetFileInfo`、PIDL 解析与 HICON 转换；图标视觉先显示轻量占位图。
- 新增单独的后台 STA Shell 图标线程，以自由图标、展开分组、收起分组三级优先级串行处理请求。
- 相同缓存键和缓存代次只提取一次；多个自由/分组视觉通过弱引用等待同一结果，避免重复 Shell 调用和控件泄漏；普通扩展名图标使用 `SHGFI_USEFILEATTRIBUTES`，不依赖首个文件仍然存在。
- 后台创建的 `BitmapSource` 在回调前冻结，随后通过 Dispatcher 安全回填；失败时继续保留文件、文件夹或系统项目占位图。
- 图标缓存主动清理时推进加载代次，丢弃旧队列与迟到结果；关闭程序时取消队列且不无限等待可能卡住的第三方 Shell 扩展。
- 所有 HICON 仍在后台线程的 `finally` 中调用 `DestroyIcon`；慢于 750 ms 的提取会写入诊断日志。
- 修正重命名迁移中按原始路径而非实际缓存键删除图标缓存的问题。
- 布局格式仍为 Version 14，不修改用户数据。

## v1.12.7 增量视觉刷新

- 移除刷新时对 `IconCanvas.Children`、自由图标表、图标视觉表和投放目标表的全量清空。
- 自由图标按显示名称、项目类型、编辑模式和图标缓存代次判断是否可复用；仅新增、删除、重命名或类型变化的项目重建控件。
- 分类框按名称、尺寸、折叠状态、分类属性、排序结果、紧凑模式和图标缓存代次生成内容指纹；位置变化只更新 Canvas 坐标。
- 普通桌面新增/删除只替换受影响视觉；未变化的右键菜单、图标控件、分类框和事件处理器保持原对象。
- 图标缓存主动清理时提升视觉代次，确保手动刷新仍能重新提取所有系统图标。
- 检测并清除拖拽中断后遗留在顶层 Canvas 的孤立分组图标，缺失子项的分类框会自动重建。
- 分类框缩放完成后立即增量重建该分类框，使列数、滚动区域和图标宽度同步更新。
- 布局格式仍为 Version 14，不修改用户数据。

## v1.12.6 Shell 桌面命名空间

- 通过 `SHGetDesktopFolder` 与 `IShellFolder.EnumObjects` 枚举 Explorer 桌面根中的回收站、此电脑、网络、用户文件和第三方 Shell 扩展。
- 使用 `SFGAO_HIDDEN` / `SFGAO_NONENUMERATED` 排除不可见项目；文件系统支持的特殊项不再被一概过滤。
- 真实桌面文件按规范化文件系统路径去重，公共桌面与用户桌面同名覆盖时仍保留完整去重集合。
- Shell 项目以 desktop-absolute parsing name 作为稳定身份，布局格式升级为 Version 14；系统语言或显示名称变化后保留位置、分组和顺序。
- Shell parsing name 使用专用编码封装，不会进入 `Path`、`File`、`Directory` 或回收站操作。
- 图标通过 PIDL 与 `SHGetFileInfo` 提取；双击打开和“属性”通过 `ShellExecuteEx` + `SEE_MASK_INVOKEIDLIST` 执行。
- 系统项目可自由摆放、网格对齐、挤压排列、加入/移出虚拟分类和参与自动分类，但不能拖入真实文件夹或删除到回收站。
- 修复异常交互恢复时仍通过 `Path.GetFileName` 查找拖动项目的问题，Shell 项目现在使用稳定的显示名称恢复预览坐标。

## v1.12.5 多显示器与 DPI 坐标模型

- 主窗口从主屏幕工作区扩展为完整虚拟桌面，支持显示器位于主屏幕左侧、上方或负坐标区域。
- 通过 `EnumDisplayMonitors` / `GetMonitorInfo` 读取每块屏幕的物理边界和独立工作区，并将物理像素转换为统一的 WPF 客户区坐标。
- 布局格式升级为 Version 13，保存显示器设备名、工作区和 DPI 快照；插拔、重排或缩放显示器时按同一设备和相对位置迁移。
- Version 12 及更早布局自动从“主屏幕工作区原点”迁移到虚拟桌面坐标，不改变主屏上的相对位置。
- 图标、分组、自动分类、智能布局、控制面板和恢复按钮均避开任务栏及显示器之间的空洞。
- 新项目默认网格位置会跳过不可用区域；智能布局可依次使用多块显示器，并仅在主屏保留下半区临时空间。
- 监听 `WM_DISPLAYCHANGE`、`WM_SETTINGCHANGE` 和 `WM_DPICHANGED`，切换显示配置前会取消未完成拖动，防止半提交状态。
- 即使首次找不到 Progman，也会在加载布局前建立完整显示器几何；稍后桌面层恢复时不会切换坐标系。

## v1.12.4 不激活桌面快捷键

- 保留 `ShowActivated=False`、`WS_EX_NOACTIVATE` 和 `MA_NOACTIVATE`，点击整理层仍不会抢占前台应用焦点。
- 新增事件驱动的 Win32 键盘/鼠标监听，使不激活窗口下的 Ctrl+Z 与 Esc 能正常执行。
- 快捷键只在 Explorer 桌面为前台，或用户最后一次鼠标按下发生在整理层时生效；点击其他应用或 Alt+Tab 后自动解除。
- 低级键盘回调只排队到 WPF Dispatcher，不在钩子内执行文件移动、布局保存或视觉重建。
- 不使用 `RegisterHotKey`；只有桌面语境中确实可执行的命令会消费对应按键，避免前台编辑器同时撤销，其他输入继续传给原应用。
- 过滤注入按键并抑制按住键产生的自动重复；Ctrl+Shift+Z、Ctrl+Alt+Z 等组合不会误触发撤销。
- 输入监听创建失败时保留面板“撤销文件移动”按钮与原 WPF `PreviewKeyDown` 后备路径。

## v1.12.3 安全模式运行时覆盖层

- 安全模式从 `AppLayoutData` 移出，只在当前进程内生效，不再写入 `layout.json`。
- 开启安全模式不再把“挤压排列”和“新项目归类”改为关闭；界面保留原开关值并临时禁用执行。
- 自动健康监测触发安全模式时不再保存布局，避免一次卡顿永久改变下次启动行为。
- 退出安全模式后重新启动 `FileSystemWatcher` 并主动刷新一次，补齐暂停期间的桌面变化。
- 进入安全模式时取消已排队的 Watcher 防抖刷新，并增加并发状态复查，避免切换瞬间重新排队。
- 旧布局中的 `SafeMode` 字段按未知字段忽略；布局格式仍为 Version 12，其他设置完全兼容。

## v1.12.2 桌面项目稳定身份与重命名迁移

- 布局格式升级为 Version 12，新增桌面项目身份表；Version 11 及更早布局自动兼容升级。
- `FileSystemWatcher.Renamed` 单独记录旧路径与新路径，程序运行中重命名会保留自由坐标、分组顺序、自动分类恢复位置和选择状态。
- 后台扫描通过 Windows 卷序列号与 128 位文件 ID 识别同一文件，程序关闭期间发生重命名后，下次启动仍可迁移原布局。
- 重命名迁移按批次提交，支持连续改名、仅修改大小写和名称交换，避免逐项覆盖造成状态丢失。
- 对硬链接或同一文件 ID 对应多个名称的歧义场景采取保守策略，不自动猜测。
- 文件身份读取失败时自动回退到实时重命名事件和普通刷新，不影响桌面显示。

## v1.12.1 项目精简

- 删除未被任何代码、XAML 或反射入口引用的 3 个 Win32 包装方法及其专用常量。
- 将 `RebuildDesktopIconsAndSaveLayout` 合并到桌面刷新模块，删除只有一个方法的 `MainWindow.Common.cs`。
- 删除未使用的 `AccentSoftBrush` 与无引用的 `ControlPanelHeader` 名称。
- 删除由 `GlobalUsings.cs` 已统一提供的重复 `using`。
- 将仅供程序集内部使用的布局模型收紧为 `internal sealed`，不改变 JSON 字段和布局 Version 11。
- 合并版本记录、验证记录和架构说明，删除重复的历史 Markdown 文件。
- 程序功能、XAML 事件处理器、布局格式、注册表项和发布方式保持不变。

## v1.12.0 完整功能架构拆分

- 以 v1.11.6 为功能基线，将大型 `MainWindow` 和 `NativeMethods` 拆分为职责明确的 partial 模块。
- 合并重复的“重建桌面视觉并保存布局”调用。
- 保留自动分类、真实文件移动、撤销、多选、排序、智能布局、安全模式、诊断、托盘和 Shell 层级修复。

## v1.11.x Shell 与窗口层级稳定性

- 修复桌面右键菜单闪烁、Windows Terminal 被整理层遮挡、Explorer 重启和外部窗口 Z 序异常。
- 引入 WinEvent 监听、线程安全事件合并、低频桌面宿主巡检和退出恢复快照。

## v1.10.0 安全与交互完善

- 增加真实文件移动撤销、分类内排序、多选、回收站操作、运行诊断和安全模式。
- 布局格式升级为 Version 10，后续 v1.11 升级为 Version 11。

## v1.9.x 桌面层与真实文件夹拖放

- 改用位于 Progman 正上方的顶级透明 WPF 窗口，避免 layered child 不可见问题。
- 增加编辑模式、自适应分类框、智能布局撤销和真实文件夹拖放。

## v1.8.x 稳定性与视觉

- 修复“整理”按钮鼠标捕获遗留、长期刷新卡顿和分类框拖动问题。
- 引入刷新防抖、后台扫描、原子布局写入、异常输入恢复和轻量 Fluent 外观。

## v1.7.0

- 支持从分类框拖出项目、跨分类移动和可保存位置的控制面板。

## v1.6.x

- 优化透明窗口命中、图标点击和拖动性能；增加桌面宿主适配、智能布局和控制栏自动收起。

## v1.5.x

- 增加原生托盘图标、单实例唤出、开机启动和自包含单文件发布脚本。

## v1.4.0

- 增加纯本地自动分类、分类预览、新项目归类和取消分类。

## v1.3.1

- 增加自由图标网格挤压预览、回滚和提交。

## v1.2.x

- 修复不激活窗口与最大化冲突，改为按工作区手动铺满。
