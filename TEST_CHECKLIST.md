# Windows 手工测试清单

## 自动化构建与测试

- [ ] 双击 `run-tests.cmd` 后，Release 构建和全部 MSTest 测试通过。
- [ ] 双击 `ci-local.cmd` 后，`artifacts\publish\win-x64\DesktopOrganizer.exe`、ZIP 和 SHA-256 文件均生成。
- [ ] 人为制造一个 C# 警告后，`ci-local.cmd` 和 GitHub Actions 都会失败。
- [ ] GitHub push 或 pull request 后，Windows CI 工作流成功上传测试结果与 win-x64 构建产物。
- [ ] 测试失败时仍能在 Actions artifact 中下载 TRX 和覆盖率文件。
- [ ] Dependabot 能识别 MSTest、Microsoft.NET.Test.Sdk、Coverlet 和 GitHub Actions 的更新。

## 启动与退出

- [ ] 首次启动后原生桌面图标隐藏，替代图标正常显示
- [ ] 点击退出后原生桌面图标恢复
- [ ] 连续启动第二个实例时不创建新进程，并展开已有实例的控制命令
- [ ] 使用任务管理器结束进程后，再次启动能够恢复并重新接管图标

## 文件与图标

- [ ] 文件、文件夹、快捷方式显示正确图标
- [ ] 双击文件可以用默认程序打开
- [ ] 双击文件夹可以打开资源管理器
- [ ] 右键“在资源管理器中显示”定位正确
- [ ] 新建、删除、重命名桌面项目后界面自动刷新
- [ ] 公共桌面快捷方式可以显示

## 布局与分组

- [ ] 自由图标可拖动，重启后位置保留
- [ ] 新建分组后可移动和缩放
- [ ] 图标拖入分组后重启仍保留
- [ ] 右键“移出分组”后图标回到自由区域
- [ ] 删除分组不会删除文件
- [ ] 图标与分组不能被拖出屏幕边缘

## 桌面交互

- [ ] 点击桌面空白区域不会遮挡其它应用
- [ ] 桌面空白区域右键菜单可正常出现
- [ ] 打开普通应用后整理层不会盖住应用窗口
- [ ] “暂停整理”显示系统原生图标
- [ ] “继续整理”重新隐藏系统原生图标

## 异常场景

- [ ] Explorer 重启后，运行中的程序能再次隐藏原生图标
- [ ] 将 layout.json 写成无效 JSON 后，程序能启动并生成损坏备份
- [ ] OneDrive 桌面重定向环境下可以扫描正确目录
- [ ] 125%、150%、200% 缩放下布局和点击位置基本正确

## 图标对齐

- [ ] 开启“网格吸附”后拖动自由图标，松开时落到最近网格。
- [ ] 两个图标拖向同一网格时，不会互相重叠。
- [ ] 图标对齐时会避开分组卡片覆盖的区域。
- [ ] 关闭“网格吸附”后，可以自由摆放图标。
- [ ] 点击“对齐图标”后，全部自由图标整齐排列并保存。
- [ ] 右键自由图标选择“对齐到网格”，仅该图标移动。
- [ ] 重启程序后，“网格吸附”开关状态和图标位置保持不变。

## 图标挤压排列

- [ ] 开启“网格吸附”和“挤压排列”后，将图标拖到另一图标所在网格，目标图标立即后移。
- [ ] 目标后面连续有多个图标时，会依次后移，直到遇到第一个空网格。
- [ ] 按住鼠标不松开并拖离目标网格，所有被挤压图标恢复到拖动开始前的位置。
- [ ] 在目标网格松开鼠标后，新顺序被保存；重启程序后顺序保持。
- [ ] 拖入分组时不会错误提交挤压预览。
- [ ] 关闭“挤压排列”后，拖到其它图标上仍使用普通的最近空闲网格对齐。
- [ ] 关闭“网格吸附”时，“挤压排列”按钮不可用。
- [ ] 分组卡片覆盖的网格会被跳过，不会把图标挤入分组卡片下方。
- [ ] 可用网格末尾没有空间时，不发生重叠或越界，原排列保持不变。
## v1.3.1 稳定性与动画

- [ ] 图标进入目标网格时，被挤压图标以短动画移动，不出现反复闪烁。
- [ ] 鼠标保持在同一目标上移动时，不会重复启动动画。
- [ ] 拖离目标网格后，被挤压图标平滑恢复原位。
- [ ] 拖动过程中 Alt+Tab 或出现系统弹窗导致鼠标捕获丢失时，拖动被取消且布局恢复。
- [ ] 抓住图标的左上、中心或右下区域拖动，目标命中均以拖动图标中心为准。


## v1.4 自动识别分类

- [ ] 点击“识别分类”后先出现类别数量预览，选择“否”不会改变布局。
- [ ] 选择“是”后仅创建虚拟自动分组，桌面真实文件路径和名称不变。
- [ ] `.lnk`、图片、文档、压缩包、代码、安装包等常见扩展名进入正确分类。
- [ ] 包含 `.git`、`.sln`、`.csproj`、`package.json`、`pyproject.toml`、`Cargo.toml` 或 `go.mod` 的桌面文件夹识别为“开发项目”。
- [ ] 普通文件夹进入“文件夹”，无法识别的文件进入“其他”。
- [ ] 已在手工分组内的项目不会被自动分类移走。
- [ ] 自动分类标题显示“· 自动”，重启后分类与开关状态保留。
- [ ] 开启“新项目归类”后，新建桌面文件会自动进入对应自动分组。
- [ ] 关闭“新项目归类”后，新建项目保持自由图标。
- [ ] 从自动分组手工移出项目后，该项目不会立刻被重新归类。
- [ ] 点击“取消分类”后自动分组消失，项目尽量恢复到分类前位置。
- [ ] “取消分类”不会改动手工分组，也不会移动或删除真实文件。
- [ ] 公共桌面和当前用户桌面同名项目仍遵循用户桌面覆盖逻辑。


## v1.5 最佳启动模式

- [ ] 双击发布目录中的 `DesktopOrganizer.exe` 可直接运行，不出现命令行窗口。
- [ ] 通知区域出现 DesktopOrganizer 图标；双击可显示/隐藏控制栏。
- [ ] 通知区域右键菜单的暂停、继续、刷新和退出均正常。
- [ ] 开启“开机自启”后，注册表 Run 项包含当前 EXE 路径和 `--startup`。
- [ ] 重新登录 Windows 后程序安静启动，控制栏默认隐藏，桌面整理功能正常。
- [ ] 控制栏隐藏时再次双击 EXE，只唤出已有实例，不产生第二个进程。
- [ ] 关闭“开机自启”后，对应 Run 项被删除。
- [ ] 重启 Explorer 后通知区域图标自动恢复。
- [ ] 点击“退出”后通知区域图标消失，系统原生桌面图标恢复。
- [ ] 双击 `publish-win-x64.cmd` 能生成自包含单文件 EXE。


## v1.6 使用者视角优化

- [ ] 打开资源管理器、浏览器或其它普通窗口后，整理分组始终位于这些窗口之后，不再覆盖应用内容。
- [ ] 回到桌面后，整理分组和紧凑控制条正常可见、可点击。
- [ ] Explorer 重启后，整理窗口可以自动重新挂载到桌面宿主。
- [ ] 无法找到 WorkerW/Progman 桌面宿主时，程序仍能以底层兼容模式运行。
- [ ] 手工启动后只显示紧凑控制条；点击“整理”展开完整命令，再次点击收起。
- [ ] 完整命令在 1366、1920、2560 像素宽度下不会横跨并溢出屏幕。
- [ ] 点击分组左侧箭头可收起/展开；重启后状态保留。
- [ ] 点击“全部收起/全部展开”后所有分组状态正确切换。
- [ ] 点击“智能布局”后分组不重叠，且不会擅自收起手工分组。
- [ ] 桌面空间不足时，“智能布局”只优先收起自动分类。
- [ ] 自动分类中的项目全部消失后，空自动分组会自动移除；手工空分组仍保留。
- [ ] 从 v1.5 的 5 个以上自动分组布局升级时，首次启动自动收起这些自动分组。
- [ ] 125%、150%、200% DPI 下桌面宿主尺寸、鼠标命中和分组边界基本正确。
- [ ] 暂停整理后系统原生图标可操作；通过托盘菜单可继续整理。


## v1.6.1 点击与性能修复

- [ ] 鼠标在桌面快速移动时，CPU 占用和界面响应明显优于 v1.6.0。
- [ ] 双击分组内图标的图片、文字、文字边缘和图标格空白处均能打开项目。
- [ ] 双击自由图标可以打开项目，不会被误判为拖动。
- [ ] 单击自由图标后不移动鼠标，不启动挤压动画，也不改变布局。
- [ ] 移动超过 Windows 系统拖动阈值后，自由图标才开始跟随鼠标。
- [ ] 按住图标拖动时，鼠标捕获稳定，不会每隔数秒突然中断。
- [ ] 大量自由图标发生挤压时，只更新实际位移项；连续跨越多个目标不明显卡顿。
- [ ] 桌面空白处仍可点击穿透，分组、图标和控制条区域保持可点击。
- [ ] 打开资源管理器或其它应用后，整理层仍位于普通窗口之后。


## v1.6.2 编译与发布

- [ ] `dotnet publish` 不再出现 CS1503。
- [ ] 单文件发布不再出现 IL3000。
- [ ] 双击发布后的 EXE 可以启动。
- [ ] 开机自启注册后可以在登录时启动。


## v1.7.0 拖出分类与总面板拖动

- [ ] 在分类框内单击图标不会移动，双击仍能打开。
- [ ] 将分类框内图标拖到桌面空白处，松手后图标成为自由图标。
- [ ] 开启网格吸附时，拖出的图标落在最近的未占用网格，并避开所有分组。
- [ ] 将分类框内图标拖到另一个分组，项目只出现在目标分组中。
- [ ] 将图标拖回原分组后不会产生重复项目。
- [ ] 拖动过程中 Alt+Tab 或鼠标捕获意外丢失，图标恢复到原分类。
- [ ] 从自动分类拖出的项目在刷新和“新项目归类”开启时不会立刻被放回。
- [ ] 从自动分类拖出的项目不会被“取消分类”覆盖新的自由位置。
- [ ] 按住总面板顶部项目统计或空白区域可以拖动面板。
- [ ] 点击总面板中的按钮不会误触发面板拖动。
- [ ] 面板拖到屏幕边缘后不会超出工作区。
- [ ] 重启程序后总面板位置保持不变。
- [ ] 展开或收起命令区后，总面板仍完整处于屏幕内。

## v1.8.0 视觉效果

- [ ] 总面板收起和展开时，圆角、双描边、顶部高光与状态胶囊显示正常。
- [ ] 面板按钮悬停、按下、禁用和开关选中状态清晰，按钮仍可正常点击。
- [ ] 不同自动分类显示不同强调色，重启后颜色保持稳定。
- [ ] 窄分类框不会让标题栏按钮越界，长名称能够省略。
- [ ] 分类框悬停时只增强边框和标题，不产生明显卡顿。
- [ ] 收起、展开、拖动、缩放、图标拖出和跨分类拖动行为与 v1.7.0 一致。

## v1.8.2 主面板输入与长期稳定性

- [ ] 点击“整理”展开后，点击项目统计、面板边缘、分区标题和命令区空白处不会再次触发“整理”。
- [ ] “整理”按钮的悬停和工具提示只在按钮自身范围内出现。
- [ ] 连续快速展开/收起 100 次后，按钮、状态区域和其它命令仍可点击。
- [ ] 展开后立即拖动总面板，只有状态胶囊区域能够开始拖动，按钮区域不会误拖动。
- [ ] 点击分类框的收起、菜单和删除按钮后，不会留下鼠标捕获；随后其它面板仍可点击。
- [ ] 在桌面连续创建、删除和重命名大量文件时，控制面板和图标拖动保持响应。
- [ ] 同一次批量复制只在操作稳定后刷新一次，不持续闪烁或反复重建分类框。
- [ ] 仅收到无实际内容变化的文件通知时，分组和图标不会周期性闪烁。
- [ ] FileSystemWatcher 发生错误后，等待数秒可继续检测新的桌面项目。
- [ ] 连续拖动、缩放、收起、挤压排列 10 分钟后，按钮仍能立即响应。
- [ ] 程序持续运行至少 30 分钟，反复打开资源管理器和修改桌面项目后不出现全局无响应。
- [ ] Alt+Tab、弹出系统对话框或拖动中断后，图标/分组恢复且不会阻塞后续刷新。
- [ ] 高频拖动后 `layout.json` 仍为有效 JSON，重启可恢复最后布局。
- [ ] 发生未处理 UI 异常时，程序退出并尝试恢复系统原生桌面图标，不留下半失效整理层。


## v1.8.3 面板恢复与分类列数

- [ ] 点击总面板右上角 `×` 后，总面板消失但右上角出现“显示面板”。
- [ ] 点击“显示面板”后，总面板恢复，恢复按钮消失。
- [ ] 隐藏、恢复重复 20 次，按钮均可点击且没有遗留鼠标捕获。
- [ ] 双击托盘图标或再次启动已有实例，仍可恢复总面板。
- [ ] 开机安静启动时不主动出现总面板和恢复按钮。
- [ ] 宽度为 280 的分类框显示垂直滚动条时仍能排成三列。
- [ ] 将分类框缩窄到 260 以下后可自然退化为两列，图标不重叠。
- [ ] 自由图标的网格尺寸、挤压排列和拖动手感保持不变。


## v1.8.4 桌面底层回归

- 启动程序时，窗口不应先闪现在资源管理器、浏览器或终端上方。
- 保持程序运行并打开资源管理器、浏览器、记事本，它们必须始终覆盖分类框和总面板。
- 在资源管理器窗口覆盖分类框的位置点击，点击必须落到资源管理器，不得触发下方分类框。
- 返回桌面后，分类框、图标和总面板仍可正常点击和拖动。
- 修改分辨率或显示缩放后，整理层仍从 WorkerW 客户区 `(0,0)` 开始且尺寸正确。
- 重启 Explorer 后观察程序是否自动重新附着；若主窗口因系统销毁而退出，重新启动程序后应再次进入 WorkerW 模式。
- 新建分组和重命名对话框应显示在屏幕中央，不应被压在桌面层。


## v1.9.0 编辑模式与自适应布局

- [ ] 从 v1.8.x 升级后默认显示“编辑布局”，分类框不能被误拖或误缩放。
- [ ] 锁定模式下双击自由图标和分类内图标仍能正常打开。
- [ ] 开启“编辑布局”后，可拖动分类框、拖动图标、缩放和从菜单删除分组。
- [ ] 点击“完成编辑”后，缩放抓手与分组菜单隐藏。
- [ ] 1 个图标的分组为单列紧凑高度；2 个图标为双列；3 个以上优先三列。
- [ ] 超过 9 个图标的三列分组出现垂直滚动条，分类框不会无限增高。
- [ ] 手动缩放后刷新桌面，分类框尺寸保持；菜单“自动适应尺寸”后重新按内容变化。
- [ ] 编辑模式下双击标题栏可自动适应尺寸，双击标题按钮不会误触。
- [ ] “智能布局”采用最短列排列，无明显重叠，并避开右上角紧凑总面板。
- [ ] 智能布局后“撤销布局”可恢复所有现存分组的位置、尺寸和收起状态。
- [ ] 开启“面板自动收起”，展开命令后把鼠标移开 6 秒，命令区自动收起。
- [ ] 鼠标停留在总面板内或正在拖动总面板时，不会自动收起。

## v1.9.1 桌面宿主可见性

1. 退出旧版本，启动 v1.9.1，确认分类框和总面板位于壁纸之上并立即可见。
2. 打开资源管理器、浏览器和终端，确认这些普通窗口始终覆盖整理层。
3. 按 Win+D 返回桌面，确认整理层重新可见。
4. 在 Explorer 尚未完全启动时运行程序，确认找不到可靠宿主时不会隐藏系统桌面图标。
5. 重启 Windows Explorer 后等待最多 15 秒，确认程序重新附着桌面层并继续可见。
6. 退出程序，确认系统桌面图标恢复。


## v1.9.2 Progman 顶级 Z 序模式

1. 完全退出旧版本，启动 v1.9.2，确认分类框和总面板立即出现在壁纸上。
2. 打开资源管理器、浏览器和终端，确认它们全部覆盖 DesktopOrganizer。
3. 最小化全部应用，确认 DesktopOrganizer 重新可见。
4. 点击分类框和总面板，确认应用窗口不会被 DesktopOrganizer 覆盖。
5. 隐藏并恢复总面板，确认恢复后仍位于桌面层。
6. 重启 Explorer，等待最多 15 秒，确认整理层恢复且不跑到壁纸后。
7. 开启 Windows Spotlight/桌面搜索后重启，确认不依赖任意 WorkerW。


## v1.9.3 分类内容操作

1. 关闭“编辑布局”，从分类框按住一个图标拖到桌面空白处，确认成为自由图标。
2. 关闭“编辑布局”，把分类框内图标拖到另一个分类框，确认完成跨组移动。
3. 右键分类框内图标，确认显示“移出分类框（不删除文件）”。
4. 点击“移出分类框”，确认真实文件仍在桌面目录中。
5. 右键项目选择“删除到回收站…”，选择“否”，确认项目未被删除。
6. 再次删除并选择“是”，确认项目进入 Windows 回收站，分类框和布局同步移除。
7. 双击分类框内图标，确认仍然正常打开，不会误触发拖动。
8. 单击并轻微移动但不超过系统拖动阈值，确认不会意外移出。

## v1.9.4 真实文件夹拖放

1. 在布局锁定状态，把自由快捷方式拖到桌面真实文件夹图标上；文件夹出现绿色高亮，松手后快捷方式从桌面消失并出现在目标文件夹。
2. 在布局锁定状态，把自由图标拖到空白处；松手后应恢复原位置。
3. 开启编辑布局，把自由图标拖到空白处；仍可正常调整布局坐标。
4. 把分类框内项目拖到真实文件夹图标；真实文件被移动，分类记录同步移除。
5. 把项目拖到虚拟分类框空白区域；应加入虚拟分类，不移动真实文件。
6. 真实文件夹图标位于分类框内时，把项目精确拖到该图标；应移动真实文件，而不是仅加入分类框。
7. 目标文件夹存在同名项目时，程序提示冲突，源项目保持原位且不覆盖目标。
8. 尝试把文件夹拖到自身或其子文件夹；操作被拒绝。
9. 拖动过程中 Alt+Tab 或失去鼠标捕获；目标高亮消失，项目恢复原位置/原分类。
10. 移动完成后刷新和重启程序；布局中不应出现已经移走的幽灵图标。


## v1.10.0 安全与交互完善

1. 1、2、3、4、7、10 个项目的分类框分别验证列数、高度和滚动条。
2. 拖一个项目到真实文件夹，确认绿色高亮和“撤销文件移动”可用。
3. 点击撤销，确认文件回到原桌面路径并恢复原分类或自由坐标。
4. 目标存在同名项目时选择自动重命名，确认不覆盖已有项目。
5. 拖到虚拟分类框时确认蓝色高亮，真实文件路径保持不变。
6. 分类菜单依次测试名称、类型、修改时间、创建时间、文件夹优先、自定义顺序，重启后保持。
7. Ctrl+单击跨分类选择多个项目，右键批量移出分类；真实文件不移动。
8. Ctrl+单击多选后批量删除到回收站，只弹一次确认；Esc 可清除选择。
9. 打开诊断日志，确认日志目录可访问；慢扫描/慢重建会写入记录。
10. 手动开启安全模式，确认新项目监听、自动归类、挤压动画停止，手动刷新仍可用。
11. 关闭安全模式，确认 FileSystemWatcher 恢复。
12. 连续运行 30 分钟并批量创建/删除桌面项目，按钮与拖动持续响应。


## v1.11.0 布局密度与细节优化

- [ ] 开启“紧凑分类框”后，1 项分类明显缩小，13 项以上分类显示四列。
- [ ] 关闭“紧凑分类框”后恢复舒展尺寸，图标不重叠。
- [ ] 点击“适应全部”后，手动锁定尺寸被解除并重新计算。
- [ ] 开启“保留临时区”执行智能布局，主屏底部约三分之一优先保持空白。
- [ ] 分类框之间无重叠，并避开右上角总面板。
- [ ] 大分类只显示垂直滚动条，不出现水平红色或系统强调色滑块。
- [ ] 锁定模式下菜单和分类徽标平时隐藏，悬停标题栏后出现。
- [ ] 旧 Version 10 布局升级后项目、分组和文件移动记录不受影响。

## v1.11.4 桌面右键启动外部应用

- [ ] DesktopOrganizer 运行时，在桌面空白处右键，菜单显示期间整理层临时隐藏，不影响菜单点击。
- [ ] 选择“在终端中打开”后，Windows Terminal 能立即出现在整理层上方。
- [ ] 选择“通过 Code 打开”后，Visual Studio Code 能正常显示。
- [ ] 取消右键菜单后，整理层在约 0.7 秒内恢复。
- [ ] 右键菜单异常停留或第三方 Shell 不发送关闭事件时，整理层最迟 15 秒自动恢复。
- [ ] 打开资源管理器内的右键菜单不会导致程序退出或长期消失。
- [ ] 连续快速打开/关闭桌面右键菜单，整理层不会闪到外部应用上方。


## v1.11.5 外部应用与长期稳定性回归

- [ ] DesktopOrganizer 运行时，在桌面右键选择“在终端中打开”，整理层不隐藏，Terminal 正常出现并位于整理层上方。
- [ ] Terminal 已经运行时再次执行“在终端中打开”，复用窗口也能显示到前面。
- [ ] Terminal 最小化后执行桌面右键命令，Shell 能正常恢复或创建终端窗口。
- [ ] 右键菜单直接取消，分类框不闪烁、不消失，也不抢焦点。
- [ ] 在资源管理器文件夹内右键，不会触发桌面整理层隐藏或异常重排。
- [ ] 测试“通过 Code 打开”、浏览器、设置、资源管理器，所有普通应用均位于整理层上方。
- [ ] 连续快速打开/关闭桌面菜单 30 次，按钮和拖动保持响应。
- [ ] 连续打开大量系统弹窗或通知后，日志目录不会每秒持续增长。
- [ ] 程序启动后立即从托盘退出，系统桌面图标恢复，无后台异常。
- [ ] 布局文件被占用或磁盘写入异常时退出不长期卡住；下次启动可恢复 `layout.pending-exit.json`。
- [ ] 连续运行 30 分钟并反复启动 Terminal/Explorer，UI Dispatcher 无明显延迟。


## v1.11.6 桌面右键无闪烁回归

1. 连续在桌面空白处右键 20 次，分类框和总面板不得消失、变暗或闪烁。
2. 打开右键菜单并停留超过 15 秒，整理层不得被巡检重排。
3. 依次展开“查看”“排序方式”“新建”子菜单，桌面内容不得闪烁。
4. 取消菜单后，分类框保持原位置和透明度。
5. 点击“在终端中打开”，Terminal 应位于整理层上方。
6. 从菜单打开 VS Code 后，VS Code 应位于整理层上方。
7. 连续右键与取消后，按钮、拖动和双击图标仍正常。

## v1.12.0 完整功能重构回归

- [ ] 使用旧版 Version 11 `layout.json` 启动，分类框位置、尺寸、排序和面板位置不变。
- [ ] 自动分类、新项目归类、取消分类均正常。
- [ ] 自由图标拖动、网格吸附和挤压排列预览/回滚/提交正常。
- [ ] 分类内拖出、跨分类移动、真实文件夹投放和撤销文件移动正常。
- [ ] Ctrl 多选、批量移出分类和批量删除到回收站正常。
- [ ] 智能布局、保留临时区、适应全部和撤销布局正常。
- [ ] 编辑模式、分类框移动/缩放/收起/排序/重命名/删除正常。
- [ ] 总面板拖动、隐藏、恢复、自动收起和托盘菜单正常。
- [ ] 桌面右键菜单无闪烁；“在终端中打开”窗口显示在整理层上方。
- [ ] Explorer 重启后托盘和桌面宿主自动恢复。
- [ ] 正常退出、异常退出和 `layout.pending-exit.json` 恢复路径正常。

## v1.12.2 重命名与稳定身份回归

- [ ] 将一个自由图标重命名，坐标保持不变，重启后仍保持。
- [ ] 将分组内项目重命名，仍留在原分组和原顺序，分组不会出现旧名称或重复项。
- [ ] 对自动分类项目重命名，再执行“取消分类”，能恢复到分类前坐标。
- [ ] 选中一个项目后重命名，选择状态迁移到新名称。
- [ ] 仅改变文件名大小写，界面标签、布局 JSON 和重启后的名称大小写一致。
- [ ] 连续执行 `A -> B -> C`，最终 C 保留 A 的位置和分组。
- [ ] 使用临时名称交换 A 与 B，两个项目的布局随各自文件身份交换，不互相覆盖。
- [ ] 关闭程序，重命名自由图标与分组项目，再启动；原位置和分组通过文件 ID 恢复。
- [ ] 旧 Version 11 布局首次启动后升级为 Version 12，所有旧坐标、分组、排序和设置不变。
- [ ] 删除文件后创建同名新文件，新文件不会错误继承已删除文件的稳定身份。
- [ ] 公共桌面和用户桌面都存在项目时，普通刷新与重命名不会抛出异常。
- [ ] 在不支持文件 ID 或无权限读取元数据的路径上，程序仍可显示项目并允许手动刷新。


## v1.12.3 安全模式运行时覆盖层回归

- [ ] 先开启“挤压排列”和“新项目归类”，再开启安全模式；两个开关仍显示为开启，但控件暂时不可操作。
- [ ] 安全模式期间拖动自由图标，不出现挤压预览；手动刷新仍可扫描当前桌面。
- [ ] 安全模式期间新增、删除或重命名桌面项目，界面不因 Watcher 自动刷新。
- [ ] 关闭安全模式后 Watcher 恢复，并在一次主动刷新后显示暂停期间的变化。
- [ ] 关闭安全模式后，原先开启的挤压排列和新项目归类恢复执行。
- [ ] 先关闭上述两个偏好，再切换安全模式；退出后仍保持关闭，不被错误开启。
- [ ] 开启安全模式后退出程序，再次启动时默认处于实时模式，两个用户偏好保持切换前值。
- [ ] 在旧 `layout.json` 中写入 `"SafeMode": true`，启动后仍处于实时模式，其他 Version 12 数据不变。
- [ ] 模拟连续三次健康警告触发自动安全模式，确认不会改写布局文件中的用户偏好。
- [ ] 安全模式切换瞬间高频创建文件，确认不会出现 Watcher 重新排队、重复刷新或异常。

## v1.12.4 不激活桌面快捷键回归

- [ ] 在任意普通应用保持前台时，点击 DesktopOrganizer 图标后按 Ctrl+Z，可撤销最近一次真实文件移动；普通应用窗口没有被整理层激活或遮挡。
- [ ] Ctrl+单击选择一个或多个桌面项目后按 Esc，选择高亮被清除。
- [ ] 点击记事本/浏览器/IDE 后按 Ctrl+Z，只触发该应用自身撤销，不改变 DesktopOrganizer 文件移动历史。
- [ ] 从整理层通过 Alt+Tab 或 Win+Tab 切换到其他应用，再按 Ctrl+Z，不触发 DesktopOrganizer。
- [ ] 点击桌面空白处使 Explorer 桌面成为前台，Ctrl+Z/Esc 仍按当前 DesktopOrganizer 状态工作。
- [ ] 在普通资源管理器文件夹窗口中按 Ctrl+Z，不会被误判为桌面命令。
- [ ] 按住 Ctrl+Z 不会连续快速撤销多条记录；松开后再次按下才撤销下一条。
- [ ] Ctrl+Shift+Z、Ctrl+Alt+Z、Win+Z 不触发 DesktopOrganizer 撤销。
- [ ] 输入法候选窗口、系统菜单和 WPF 重命名对话框中按 Esc，不造成异常或重复命令。
- [ ] 使用屏幕键盘或自动化工具发送注入按键时，不触发真实文件撤销。
- [ ] 连续点击整理层与外部应用 100 次并交替按 Esc/Ctrl+Z，快捷键语境不串到外部应用。
- [ ] 退出程序后，任务管理器中进程完全结束，其他应用键盘输入不受影响。



## v1.12.5 多显示器与 DPI 回归

- [ ] 单屏 100% 缩放启动，原有图标、分组和控制面板位置不变。
- [ ] Version 12 布局升级为 Version 13 后，主屏相对位置不变，JSON 新增 `DesktopTopology`。
- [ ] 第二块显示器位于主屏右侧、左侧、上方和下方时，整理层覆盖全部屏幕。
- [ ] 左侧/上方显示器产生负系统坐标时，图标可拖入并在重启后保留位置。
- [ ] 两块显示器分别使用 100% 与 150%/200% 缩放，命中、拖动、分组缩放和双击打开不偏移。
- [ ] 每块显示器任务栏位于上、下、左、右时，图标、分组和控制面板不会进入任务栏区域。
- [ ] 显示器尺寸不同或上下错位时，不会把图标放入两屏之间不存在的矩形空洞。
- [ ] 在副屏放置自由图标、手工分组和自动分类，重启后仍位于同一显示器。
- [ ] 运行中交换显示器左右顺序，同一设备上的布局按相对位置迁移。
- [ ] 运行中把某显示器缩放从 100% 改为 150%，布局不消失、不越界，控制面板仍可点击。
- [ ] 断开副屏后，其项目迁移到主屏工作区；重新连接后不会落入任务栏或屏幕空洞。
- [ ] 隐藏总面板后，“显示面板”按钮出现在原面板所在显示器的可用右上角。
- [ ] 智能布局可使用多块显示器，且“保留临时区”只优先保留主屏底部约三分之一。
- [ ] 显示配置变化发生在拖动过程中时，拖动被安全取消，不提交半完成位置或文件移动。
- [ ] 连续插拔/重排显示器 20 次，布局文件仍是有效 JSON，程序无异常退出。


## v1.12.14 全桌面图标防闪烁回归

- [ ] 程序空闲运行 10 分钟，所有自由图标与分类框内图标保持稳定，不周期性消失、重现或回到占位图。
- [ ] 开启 Windows 壁纸轮播并连续切换 20 次，整理图标不整体闪烁，坐标和分类不变化。
- [ ] 切换浅色/深色主题、强调色和透明效果，普通 `WM_SETTINGCHANGE` 不触发整层重建。
- [ ] 连续打开、关闭、最小化浏览器、终端和 WinUI 应用 50 次，整理层不因 `EVENT_OBJECT_SHOW` 风暴闪烁，外部应用仍显示在整理层上方。
- [ ] 打开和关闭桌面右键菜单、通知中心、开始菜单与搜索，图标不闪烁，菜单可正常交互。
- [ ] 改变分辨率、缩放比例、主显示器和任务栏位置，布局只迁移一次；完成后不持续刷新。
- [ ] 临时让用户桌面目录不可读或模拟枚举中途 I/O 失败，当前图标保持原样；恢复后后台重试同步真实变化。
- [ ] 重启 Explorer 或暂时使 Shell 命名空间枚举失败，回收站不会每轮消失和重现。
- [ ] 模拟异步 Shell 图标加载返回空结果，已经显示的真实图标不退回占位图。
- [ ] 连续触发 20 次无内容变化的手动刷新，视觉对象保持复用且不发生整屏闪烁。
- [ ] 检查诊断日志：正常空闲期间不持续出现 `LAYER corrected`、`SCAN incomplete` 或显示配置更新循环。

## v1.12.13 Shell 桌面项目白名单回归

- [ ] 在 Windows“桌面图标设置”中同时启用回收站、此电脑、网络和用户文件，整理层中只显示回收站。
- [ ] 安装 WSL 后，Linux 图标不出现在整理层桌面。
- [ ] 控制面板、库、文档、下载、音乐、图片及第三方 Shell 扩展不出现在整理层桌面。
- [ ] 用户桌面或公共桌面中的真实文件、文件夹和 `.lnk` 快捷方式仍全部显示，不被白名单误删。
- [ ] 双击回收站可正常打开；右键“属性”可打开系统提供的属性页。
- [ ] 回收站可自由拖动、网格对齐、挤压排列、加入分类框、跨分类移动和移出分类框。
- [ ] 回收站不能拖入真实文件夹，也不显示“删除到回收站”菜单。
- [ ] 从 v1.12.12 升级后，旧布局中的此电脑、网络、Linux 等项目在首次刷新时消失，相关空自动分类框被清理。
- [ ] 旧 Shell 项目被清理后重启程序，不会再次出现，也不会在 `layout.json` 的 `ItemIdentities` 中残留。
- [ ] 系统语言切换为英文或其他语言后，仍通过 CLSID 识别并只保留本地化名称的回收站。
- [ ] 手动刷新图标缓存后，回收站真实图标重新回填；失败时仅显示占位图，不影响 UI。
- [ ] 布局为 Version 15，升级前的真实文件位置、分组、显示器拓扑和设置保持不变。

## v1.12.7 增量视觉刷新回归

- [ ] 桌面无变化时连续点击刷新 20 次，图标和分类框不闪烁，右键菜单对象保持可用。
- [ ] 新增一个自由文件后，只出现该新图标；原有图标的悬停、选择和右键菜单状态不异常。
- [ ] 删除一个自由文件后，只移除对应图标，其他图标坐标不变化。
- [ ] 重命名自由图标和分类内图标后，只替换受影响视觉，位置、分类和排序保持。
- [ ] 移动一个自由图标或分类框位置后，视觉对象不重建，拖动结束无闪烁。
- [ ] 调整一个分类框宽高后，该分类框立即重新计算列数和滚动区，其他分类框不重建。
- [ ] 切换编辑模式时，自由图标和分类框正确更新菜单与操作按钮。
- [ ] 切换紧凑分类布局时只重建分类框，自由图标保持。
- [ ] 对按修改时间或创建时间排序的分类修改文件时间，再触发刷新，顺序正确更新。
- [ ] 点击手动刷新清空图标缓存后，所有图标重新提取，快捷方式或 EXE 更新后的图标可见。
- [ ] 分类内拖动过程中强制丢失鼠标捕获，图标回到正确分类，不留下顶层孤立图标。
- [ ] 100、500、1000 个桌面项目下连续新增/删除单个文件，UI 无整屏闪烁，刷新耗时明显低于 v1.12.6。
- [ ] 多显示器、回收站、真实文件夹投放、挤压排列与安全模式回归全部通过。
- [ ] 连续增删改名 500 次后，`_allIconVisuals`、文件夹投放目标与 Canvas 实际视觉数量一致。


## v1.12.8 Shell 图标后台加载回归

- [ ] 冷启动包含 100、500、1000 个项目的桌面，图标先显示占位图，控制面板、拖动和右键操作立即可响应。
- [ ] 图标逐步回填时，项目坐标、选择高亮和分类框尺寸不跳动。
- [ ] 自由图标先于展开分类中的图标出现；收起分类中的图标最后加载。
- [ ] 多个同扩展名普通文件只执行一次扩展图标提取，并同时更新所有对应视觉。
- [ ] 同一个项目同时经历分类移入/移出和增量重建时，不出现旧图标回填到新项目或已删除控件。
- [ ] 点击“刷新桌面项目”清空缓存后，旧代次迟到结果不回填，当前代次重新提取图标。
- [ ] 快速连续刷新 20 次，后台队列不线性增长，内存不会因等待控件强引用持续增加。
- [ ] 重命名 `.exe`、`.lnk`、`.url`、`.ico` 后，新路径图标正确刷新，旧路径缓存不再被误保留。
- [ ] 回收站、文件夹、快捷方式、EXE 和常见扩展名文件均能回填真实图标。
- [ ] 模拟回收站图标提取失败，项目持续显示占位图但可打开、拖动和使用菜单。
- [ ] 模拟单次图标提取超过 2 秒，UI 不冻结，诊断日志记录 `SLOW operation=shell-icon-load`。
- [ ] 图标加载过程中删除项目、切换安全模式、改变显示器拓扑和重启 Explorer，不发生跨线程异常。
- [ ] 图标加载过程中退出程序，窗口关闭不被第三方 Shell 扩展无限阻塞，进程最终退出。
- [ ] 使用 Process Explorer 连续刷新 100 次，HICON/GDI 句柄没有持续增长。


## v1.12.9 分类框可视区域虚拟化回归

- [ ] 创建包含 500、1000 个项目的单个展开分类框，首次显示时只创建可视行与缓冲行，控制面板和其它桌面图标仍可立即操作。
- [ ] 连续滚动到分类框顶部、中央和底部，项目顺序、图标标签、右键菜单和双击打开均正确。
- [ ] 快速滚轮滚动 100 次，不出现空白永久残留、重复项目、重叠行或滚动条跳回顶部。
- [ ] 滚出视口再滚回，同一项目图标从缓存快速恢复；异步 Shell 图标不会写入错误的复用控件。
- [ ] 收起包含大量项目的分类框后，已实现子图标数量降为零或不再增加；再次展开只加载当前视口。
- [ ] 在分类框中间位置滚动后移动、缩放或触发无内容变化的刷新，运行时滚动位置尽量保持。
- [ ] 从分类框当前可见区域拖出图标到自由桌面、其它分类框和真实文件夹，拖动期间不出现原位置重复图标。
- [ ] 分类图标拖动中强制丢失鼠标捕获，原分类完整恢复，没有被永久抑制或遗漏的项目。
- [ ] Ctrl+单击选择多个项目后滚动，使部分已选项目虚拟化；批量移出分类和批量删除真实项目仍按选择集合执行。
- [ ] 按名称、类型、修改时间、创建时间和文件夹优先切换排序，虚拟列表顺序与非虚拟版本一致。
- [ ] 改变紧凑布局、分类框宽度和列数后，Extent、滚动条范围和可见行重新计算，无越界。
- [ ] 删除、重命名或新增分类项目后，增量刷新更新完整虚拟快照，不保留旧项目容器。
- [ ] 使用诊断工具连续滚动 10 分钟，WPF 控件、ContextMenu、Image 和 HICON/GDI 句柄数量不持续线性增长。
- [ ] 小分类（1～12 项）的拖动、折叠、排序、自动适应尺寸与 v1.12.8 行为一致。


## v1.12.10 后台真实文件操作回归

- [ ] 将普通文件拖入同盘真实文件夹，鼠标松开后 UI 立即恢复响应，源图标显示半透明等待状态；成功后图标消失并可撤销。
- [ ] 将包含大量小文件的文件夹拖入真实文件夹，移动期间控制面板、滚动、右键和其它图标仍可操作。
- [ ] 在 OneDrive/网盘同步目录、杀毒软件扫描或文件被短暂占用时执行移动，UI 不冻结；失败后原位置和分类完整恢复。
- [ ] 同一个项目处于移动队列时，再次拖动、删除、移出分类或批量操作会被拒绝，不生成重复任务。
- [ ] 对不同项目连续提交多个移动/删除任务，按提交顺序串行完成，状态栏显示剩余后台任务数量。
- [ ] 两个任务尝试操作同一真实文件或同一撤销路径时，后一个任务被阻止，不发生覆盖或布局错乱。
- [ ] 目标文件夹在排队后、执行前被删除，移动失败并恢复原视觉，不写入撤销历史。
- [ ] 目标中排队后出现同名项目：未允许自动改名时安全失败；允许时生成不重复名称并记录真实目标路径用于撤销。
- [ ] 批量删除 20 个项目，其中包含只读、被占用或已消失项目；成功项从布局移除，失败/取消项保留，并显示汇总而不是中断整批。
- [ ] 回收站确认对话框取消后不创建后台任务，选择状态和布局不变。
- [ ] 移动成功后立即按 Ctrl+Z，撤销进入后台队列；执行期间快捷键和撤销按钮禁用，成功后项目回到原分类、顺序或自由坐标。
- [ ] 撤销时原位置存在同名项目，不覆盖现有项目，撤销记录保留以便用户处理冲突后重试。
- [ ] 撤销时移动后的项目已经不存在，移除失效撤销记录并给出明确状态。
- [ ] 文件任务执行期间触发桌面刷新、显示器变化、安全模式或 Explorer 重启，待处理视觉状态不丢失，完成后布局与磁盘一致。
- [ ] 提交多个任务后立即退出：尚未开始的任务被取消；活动原生操作不会造成窗口无限等待，进程最终退出。
- [ ] 连续执行 200 次移动、回收站与撤销后，`_pendingFileOperationPaths` 和任务计数归零，无永久半透明或不可点击图标。


## v1.12.11 编译修复回归

- [ ] `dotnet clean` 成功。
- [ ] `dotnet build -c Release` 为 0 错误。
- [ ] `dotnet publish -c Release -r win-x64 --self-contained false` 为 0 错误。
- [ ] 编译输出不再包含 CS0136、CS8600、CS8602。
- [ ] 文件重命名后位置和分类仍保持。
- [ ] 异步 Shell 图标仍能从占位图正常回填。
- [ ] 新增/删除桌面项目时增量刷新不重建全部视觉。


## 19. 回收站专属小组件（v1.12.15）

- [ ] 回收站只显示为独立小组件，不再出现普通图标或进入分类框。
- [ ] 状态能区分空与非空，并显示项目数和占用空间。
- [ ] 空/非空状态切换时只更新回收站图标和状态，不让其他桌面图标重建或闪烁。
- [ ] “打开”及双击卡片空白区域可以打开系统回收站。
- [ ] “清空”有明确二次确认，成功后状态立即变为空。
- [ ] 标题区域可拖动，按钮点击不会误触发拖动。
- [ ] 拖到副屏、负坐标屏或任务栏附近后会限制在可用工作区。
- [ ] 重启后位置和显示/隐藏状态保持。
- [ ] 暂停整理时组件隐藏，继续后恢复。
- [ ] 智能布局、新分类和网格对齐不会覆盖组件区域。
