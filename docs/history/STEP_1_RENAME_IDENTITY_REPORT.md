# 第一步修复报告：重命名后布局丢失

版本：DesktopOrganizer v1.12.2  
布局格式：Version 12

## 问题根因

旧版本使用桌面项目的显示名称作为自由坐标、分组成员、自动分类恢复位置和选择状态的键。文件重命名后，刷新逻辑看到“旧名称消失、新名称出现”，会先删除旧键，再给新名称创建默认布局，因此原位置和分组丢失。

## 本次实现

1. `FileSystemWatcher.Renamed` 使用独立处理器保存旧路径和新路径，不再只触发普通刷新。
2. 每次后台扫描为项目读取：
   - 最近路径；
   - 卷序列号；
   - 128 位文件 ID；
   - 创建时间；
   - 文件/目录类型。
3. 布局新增 `ItemIdentities`，旧 Version 11 文件可直接反序列化，并在首次扫描后升级为 Version 12。
4. 刷新时先建立完整的旧名称到新名称映射，再一次性迁移：
   - 自由图标坐标；
   - 分组成员与顺序；
   - 自动分类前坐标；
   - 多选状态；
   - 挤压预览状态；
   - 当前会话的文件移动撤销记录；
   - 图标缓存。
5. 批量迁移支持普通改名、仅修改大小写、连续改名以及两个名称通过临时名称交换。
6. 硬链接等“一份身份对应多个名称”的歧义情况不会自动猜测，避免错误迁移。

## 兼容性

- 不改变旧布局字段含义。
- Version 11 及更早布局缺少 `ItemIdentities` 时按空表处理，不会丢失原坐标或分组。
- 文件系统不支持文件 ID、文件暂时不可访问或权限不足时，只失去“程序关闭期间改名恢复”能力；普通显示、实时 watcher 改名和手动刷新仍可工作。
- 当前项目仍以显示名称作为大部分 UI 字典键，因此“用户桌面与公共桌面存在完全同名项目”的历史限制尚未解决。

## 验证

已执行：

- 全部 C# 文件词法括号、字符串和注释平衡检查；
- XAML、项目文件和 manifest XML 解析；
- XAML 事件处理器存在性检查；
- StaticResource 声明/引用检查；
- watcher 订阅与取消订阅配对检查；
- Version 12 与身份迁移调用链检查；
- 批量映射算法的普通改名、目标旧状态、名称交换和大小写改名模拟测试；
- ZIP CRC 与 SHA-256 校验。

当前 Linux 容器没有预装 .NET SDK，且运行环境不能启动 Windows WPF/Explorer 会话，因此真实 `dotnet build`、Win32 文件 ID 调用和桌面交互仍需在 Windows + .NET 10 环境执行。对应回归步骤已写入 `TEST_CHECKLIST.md`。

## 下一步

第二步处理安全模式：安全模式只作为运行时覆盖层，不能永久关闭用户原本的“挤压重排”和“新项目自动分类”设置。
