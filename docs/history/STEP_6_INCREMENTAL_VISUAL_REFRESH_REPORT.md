# DesktopOrganizer v1.12.7 第六阶段报告：增量视觉刷新

## 目标

解决桌面内容或布局发生轻微变化时，程序仍清空 `IconCanvas` 并重新创建全部分类框、图标、右键菜单和事件处理器所造成的卡顿与闪烁。

## 原问题

`RebuildDesktopIconsCore` 每次执行都会调用 `IconCanvas.Children.Clear()`，随后清空全部视觉索引，再为所有桌面项目重新提取/读取图标缓存、创建 WPF 控件、菜单和事件委托。桌面项目越多，单个新增或删除文件的成本越接近一次完整首屏构建。

## 实现

### 自由图标差异更新

- 保留 `_freeIconVisuals`。
- 为每个自由图标保存轻量运行时状态：项目视觉类型、编辑模式和图标缓存代次。
- 路径、显示名称或项目类型发生变化时替换该图标；仅坐标变化时复用控件并更新 `Canvas.Left/Top`。

### 分类框差异更新

- 新增 `_groupVisuals` 与内容指纹。
- 指纹覆盖名称、尺寸、折叠状态、自动分类属性、锁定状态、排序方式、实际排序后的项目及路径、编辑模式、紧凑模式和图标代次。
- 分类框仅移动位置时复用；内容或结构变化时只重建该分类框。

### 缓存一致性

- 移除视觉时递归注销 `_allIconVisuals` 和 `_physicalFolderDropTargets`。
- 刷新会清除未登记的 Canvas 顶层孤立视觉。
- 复用分类框前检查其实际子图标集合；拖拽中断造成缺项时自动重建。
- 主动清空图标缓存时增加 `_iconVisualGeneration`，确保所有图标视觉重新创建。

### 关联修复

分类框缩放结束后由仅保存布局改为执行一次增量重建，使图标列宽、WrapPanel 宽度与滚动区域立即匹配新尺寸。由于采用差异刷新，只会替换该分类框。

## 兼容性

- `AppLayoutData.Version` 仍为 14。
- 不新增或修改 JSON 字段。
- 文件身份、多显示器、Shell 命名空间、安全模式、快捷键和真实文件操作逻辑保持原入口。
- 未添加第三方 NuGet 依赖。

## 验证

已执行：

- 36 个 C# 文件的字符串、注释及括号词法平衡检查；
- XAML、项目文件与 manifest XML 解析；
- XAML 事件处理器存在性检查；
- StaticResource 引用检查；
- 禁止全量 `IconCanvas.Children.Clear()` 的源码断言；
- 自由图标/分类框缓存创建、复用、注销与图标代次源码断言；
- ZIP CRC 与 SHA-256 完整性检查。

当前容器不是 Windows WPF/Explorer 会话。虽尝试获取官方 .NET 10 SDK 进行编译，但执行环境无法下载大型 SDK 二进制，因此仍需在 Windows 上执行 `dotnet build` 和 `TEST_CHECKLIST.md` 中的性能回归。
