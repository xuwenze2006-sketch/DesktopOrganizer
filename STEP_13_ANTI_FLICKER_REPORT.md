# DesktopOrganizer v1.12.14 全桌面图标防闪烁修复报告

## 现象

v1.12.13 运行后，自由图标、分类框内图标以及回收站可能持续整体闪烁。闪烁并非单个图标资源损坏，而是全屏透明 WPF 整理层在系统广播、窗口显示事件或不完整快照下被反复调整、重建或切换。

## 根因与修复

### 1. 普通系统设置消息误触发显示器重算

旧逻辑把所有 `WM_SETTINGCHANGE` 都当作显示器几何变化。壁纸轮播、主题、颜色、辅助功能和多种 Shell 状态都会发送该消息，导致全屏窗口反复设置尺寸并重建视觉。

v1.12.14 仅接受：

- `WM_DISPLAYCHANGE`
- `WM_DPICHANGED`
- `WM_SETTINGCHANGE` 且 `wParam == SPI_SETWORKAREA`

一次变化产生的连续广播会在 180 ms 内合并。仅当窗口边界或显示器拓扑实际变化时更新坐标；不重新扫描桌面，不清空图标缓存。

### 2. 顶级窗口 SHOW 事件造成 Z 序反馈循环

浏览器、通知中心、WinUI 宿主和后台窗口会大量产生 `EVENT_OBJECT_SHOW`。旧逻辑对每个事件执行立即校正和三轮延迟复查，可能反复 `SetWindowPos` 全屏 layered window。

v1.12.14：

- 普通 SHOW 事件不再发布桌面层校正；
- 只在外部窗口真正成为前台时检查；
- 外部应用位于整理层下方时只提升外部窗口；
- 运行期不要求整理层必须紧邻 Progman，只要求仍为顶级窗口且处于 Progman 上方；
- Windows 11 桌面菜单 SHOW/HIDE 识别仍保留。

### 3. 目录枚举中途失败提交半份快照

`Directory.EnumerateFileSystemEntries` 是延迟枚举，可能在读取若干项目后抛出 I/O 或权限异常。若已读取部分内容立即写入目标字典，刷新会把其余图标当成已删除；下一次成功扫描又全部加回。

现在每个桌面目录先完整枚举到临时列表，成功后一次性提交。任何实际枚举失败都会拒绝本轮物理快照、保留当前视觉并退避重试。用户桌面或公共桌面只要有一个无法完整读取，本轮物理快照就不会提交。

### 4. Shell 枚举短暂失败被解释为回收站删除

Shell 命名空间枚举现在返回明确的完整性状态。暂时失败时保留上一轮回收站，并在后台重试；不会让回收站在每轮刷新中消失和重现。

### 5. 原生桌面图标和整理图标交替显示

桌面层巡检不再因一次 Z 序查询失败立即恢复 Explorer 原生图标。只有连续三次低频巡检确认失效后才执行安全回退，防止两套图标交替显示。

### 6. 异步空图标覆盖有效图标

后台 Shell 图标任务返回空值时，不再覆盖已经成功显示的图像。这样第三方 Shell 扩展的迟到失败不会使图标从真实图退回占位图。

## 兼容性

- 程序版本：1.12.14
- 布局格式：Version 14
- 不新增或删除布局 JSON 字段
- 真实桌面文件、文件夹和快捷方式保持不变
- Shell 虚拟项目仍只显示回收站
- 不新增运行时 NuGet 依赖

## 验证范围

完成了源码词法结构、XML/XAML、事件处理器、资源引用、Git 补丁、窗口消息过滤测试和压缩包完整性检查。当前容器没有 .NET 10 SDK，也没有 Windows Explorer/WPF 桌面会话，因此未执行真实 Windows 构建与闪烁实机回归。

实机验证重点已加入 `TEST_CHECKLIST.md`，包括壁纸轮播、主题切换、窗口 SHOW 风暴、显示器变化、目录暂时不可读、Explorer 重启和异步图标空结果。
